{{- if .Values.sense.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "neutron-os.fullname" . }}-sense
  labels:
    {{- include "neutron-os.labels" . | nindent 4 }}
    app.kubernetes.io/component: sense
spec:
  replicas: {{ .Values.sense.replicas }}
  selector:
    matchLabels:
      {{- include "neutron-os.senseSelectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "neutron-os.senseSelectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "neutron-os.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: sense
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.sense.image.repository }}:{{ .Values.sense.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.sense.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "neutron-os.fullname" . }}-config
          env:
            {{- if .Values.secrets.database }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.database }}
                  key: url
            {{- else }}
            - name: DATABASE_URL
              value: {{ include "neutron-os.databaseUrl" . | quote }}
            {{- end }}
            {{- if .Values.secrets.llm }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.llm }}
                  key: api_key
                  optional: true
            {{- end }}
            {{- range $key, $value := .Values.sense.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.sense.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.sense.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.sense.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.inbox.enabled }}
            - name: inbox
              mountPath: /app/tools/agents/inbox
            {{- end }}
            {{- if .Values.persistence.media.enabled }}
            - name: media
              mountPath: /app/tools/agents/inbox/raw
            {{- end }}
      volumes:
        {{- if .Values.persistence.inbox.enabled }}
        - name: inbox
          persistentVolumeClaim:
            claimName: {{ include "neutron-os.fullname" . }}-inbox
        {{- end }}
        {{- if .Values.persistence.media.enabled }}
        - name: media
          persistentVolumeClaim:
            claimName: {{ include "neutron-os.fullname" . }}-media
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

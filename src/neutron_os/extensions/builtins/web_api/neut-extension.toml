[extension]
name = "web-api"
version = "0.1.0"
description = "HTTP API server for neut chat — embeddable in any web page"
author = "NeutronOS"
builtin = true
kind = "service"
module = "platform"

[[cli.commands]]
noun = "serve"
module = "neutron_os.extensions.builtins.web_api.cli"
description = "Start the neut HTTP API server"

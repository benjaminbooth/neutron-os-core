[extension]
name = "mirror-agent"
version = "0.1.0"
description = "AI-powered public mirror gate — reviews content for sensitive data before publishing"
builtin = true
kind = "agent"
module = "platform"

[[cli.commands]]
noun = "mirror"
module = "neutron_os.extensions.builtins.mirror_agent.cli"

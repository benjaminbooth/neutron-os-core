[extension]
name = "dfib"
version = "0.1.0"
description = "D-FIB (Defib) — diagnostics and system health"
author = "Neutron OS Team"
builtin = true
kind = "agent"
module = "platform"

[cli]
noun = "doctor"
